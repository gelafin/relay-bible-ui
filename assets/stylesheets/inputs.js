import { StyleSheet } from 'react-native';

const inputStyle = StyleSheet.create({
  default: {
    padding: '5px',
    backgroundColor: '#eee',
    borderRadius: '5px',
  }
});

export { inputStyle };
