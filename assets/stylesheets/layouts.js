import { StyleSheet } from 'react-native';

const layoutStyles = StyleSheet.create({
  page: {
    flex: 1,
    backgroundColor: '#fff',
  },
  pageMargins: {
    marginLeft: '20px'
  }
});

export { layoutStyles };